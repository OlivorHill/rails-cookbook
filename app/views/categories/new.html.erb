<h3>Create a new Category</h3>

<%= simple_form_for(@category) do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>

<h5> <%= link_to "Back to Categories", categories_path, class: "btn btn-primary text-white" %> </h5>
